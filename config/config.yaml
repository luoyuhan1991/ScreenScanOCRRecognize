# 用户改动后的覆盖值。空文件等同于全部使用 defaults.py 的 DEFAULT_CONFIG。
# 用户在 GUI 修改后会自动写入；这里也可以手动编辑。
scan:
  interval_seconds: 3.0
  enable_roi: true
  saved_roi:
  - 1170
  - 256
  - 1880
  - 843
matching:
  match_ratio_threshold: 0.85
